const a = Math.pow(2, 1-1)
const b = Math.pow(2, 2-1)
const c = Math.pow(2, 3-1)
const d = Math.pow(2, 4-1)
const e = Math.pow(2, 5-1)
const f = Math.pow(2, 6-1)
const g = Math.pow(2, 7-1)

const prob = [a, b, c, d, e, f, g]

const min = 124;

const sumofprobnums = a + b + c + d + e + f + g


document.getElementById('num').innerHTML = sumofprobnums;
